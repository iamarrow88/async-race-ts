/* describe('When run this function', () => {
  it('returns object contains list of winners', () => {
    const result = add(10, 5);
    const expected = 15;

    expect(result).toEqual(expected);
  })
}) */